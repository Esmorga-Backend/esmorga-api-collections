info:
  name: Login Current User
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/v1/account/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "email": "{{user}}",
          "password": "{{password}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        bru.setEnvVar("accessToken", body.accessToken);
        bru.setEnvVar("refreshToken", body.refreshToken);

        bru.setEnvVar("userRole", body.profile.role);
        bru.setEnvVar("loggedUserEmail", body.profile.email);

        bru.setEnvVar(
          "userFullName",
          `${body.profile.name} ${body.profile.lastName}`
        );
    - type: tests
      code: |-
        const body = res.getBody();

        test("Status code is 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Access token is returned", function () {
          expect(body.accessToken).to.be.a("string");
          expect(body.accessToken).to.not.be.empty;
        });

        test("Refresh token is returned", function () {
          expect(body.refreshToken).to.be.a("string");
          expect(body.refreshToken).to.not.be.empty;
        });

        test("TTL is 600", function () {
          expect(body.ttl).to.equal(600);
        });

        test("Profile has expected data", function () {
          expect(body.profile).to.have.property("name");
          expect(body.profile).to.have.property("lastName");
          expect(body.profile).to.have.property("email");
          expect(body.profile).to.have.property("role");
        });

        test("Logged user has a valid role", function () {
          expect(["USER", "ADMIN"]).to.include(body.profile.role);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: QA login
    request:
      url: "{{port}}{{host}}/v1/account/login"
      method: POST
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
              "email": "esmorgaqa8@yopmail.com",
              "password": "Mobgen!1"
          }
    response:
      body:
        type: text
        data: ""
  - name: PROD login
    request:
      url: "{{port}}{{host}}/v1/account/login"
      method: POST
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
              "email": "esmorga15101@yopmail.com",
              "password": "Mobgen!1"
          }
    response:
      body:
        type: text
        data: ""
  - name: Local login
    request:
      url: "{{port}}{{host}}/v1/account/login"
      method: POST
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
              "email": "testdelete@yopmail.com",
              "password": "Mobgen1!"
          }
    response:
      body:
        type: text
        data: ""
